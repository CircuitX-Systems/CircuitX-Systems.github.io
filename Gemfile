# This line specifies where to find the gems.
source "https://rubygems.org"

# This line ensures the Jekyll gem itself is installed.
gem "jekyll"

# This group contains all the plugins that GitHub Pages supports.
# Using the 'github-pages' gem is the best practice for compatibility.
group :jekyll_plugins do
  gem "github-pages"
end
